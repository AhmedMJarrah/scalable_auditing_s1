# scalable_auditing_s1 — Python 3.11
# Install once, so the venv does not need rebuilding at every step.
#   pip install -r requirements.txt

# --- step 1: config + logging -----------------------------------------
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0

# --- step 2: audit-type specs -----------------------------------------
PyYAML>=6.0

# --- database ----------------------------------------------------------
SQLAlchemy>=2.0,<3.0
alembic>=1.13
# Uncomment when moving to Postgres on the server:
# psycopg[binary]>=3.1

# --- data / sampling / scoring ----------------------------------------
pandas>=2.2
numpy>=1.26
scipy>=1.13          # confidence intervals, agreement statistics

# --- web (FastAPI + Jinja2 + HTMX) ------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9      # form submissions
itsdangerous>=2.2            # signed session cookies

# --- auth ---------------------------------------------------------------
# passlib intentionally NOT used: it is incompatible with bcrypt>=4.0
# (passlib expects an internal attribute bcrypt 4.x removed). We call
# bcrypt directly instead — see src/assignment/auth.py.
bcrypt>=4.1

# --- google sheets mirror ---------------------------------------------
gspread>=6.1
google-auth>=2.30

# --- charts ------------------------------------------------------------
matplotlib>=3.9

# --- dev ---------------------------------------------------------------
pytest>=8.2
pytest-cov>=5.0
ruff>=0.5
